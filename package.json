{
  "name": "pantrypilot",
  "version": "1.0.0",
  "description": "PantryPilot - AI-powered grocery assistant deployable to Cloudflare Workers",
  "main": "src/worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "node build.js",
    "test": "vitest",
    "format": "prettier --write '**/*.{js,json,md}'",
    "lint": "eslint src/**/*.js"
  },
  "keywords": [
    "safeway",
    "mcp",
    "cloudflare",
    "workers",
    "grocery",
    "ai"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240129.0",
    "wrangler": "^3.50.0",
    "vitest": "^1.2.0",
    "prettier": "^3.2.0",
    "eslint": "^8.56.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
